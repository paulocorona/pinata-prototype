{
  "name": "pinata-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Piñata — active incremental Three.js prototype",
  "scripts": {
    "dev": "vite",
    "dev:ensure": "node scripts/ensure-dev.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "screenshot": "node scripts/screenshot.mjs"
  },
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "playwright": "^1.62.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
